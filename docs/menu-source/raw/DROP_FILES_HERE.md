# Drop raw menu source here

Put source material for transcription in this folder:

- **Menu photos** — the complete Breakfast, Lunch, Maharani and Maharaja menus
  (any common image format: .jpg / .png / .heic / .webp).
- **Master PDF** — the full à-la-carte dish list for the custom menu builder.

Once these are here (and `../set-menus.csv` has the 3 new packages), I'll
transcribe the images + PDF into `../set-menus.csv` and `../custom-dishes.csv`,
then build the importer. Any filename is fine.
